deployment:
  tasks:
    - export DEPLOY_PATH=/home/nikusoft/public_html/historical-calendar.onespect.com/beta2
    - /bin/echo "Starting deployment to $DEPLOY_PATH"
    - cd $DEPLOY_PATH
    - /usr/bin/git fetch origin
    - /usr/bin/git reset --hard origin/main
    - /bin/echo "Deployment completed on $(date)"

